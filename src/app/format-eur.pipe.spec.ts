import { FormatEurPipe } from './format-eur.pipe';

describe('FormatEurPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEurPipe();
    expect(pipe).toBeTruthy();
  });
});
